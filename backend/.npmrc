# NPM configuration for backend
# This ensures Prisma client generation happens via our postinstall script
# rather than Prisma's default postinstall

# Disable Prisma's automatic binary downloads during install
# We handle client generation via our package.json postinstall script
prisma-client-skip-postinstall=false
